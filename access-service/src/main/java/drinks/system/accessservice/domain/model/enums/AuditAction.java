package drinks.system.accessservice.domain.model.enums;

/**
 * Acciones de auditoría que pueden ser registradas en el log de auditoría del sistema.
 */
public enum AuditAction {
    CREATE,
    UPDATE,
    DELETE,
    LOGIN,
    LOGOUT
}
