package drinks.system.accessservice.domain.model.enums;

/**
 * Tipos de datos soportados para los valores de parámetros del sistema.
 */
public enum DataType {
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    JSON
}
